CONTACT IMMINENT v0.4.2 — CLEAN PAUSE + MENU REBUILD

Rebuilt directly from frozen v0.3.5, not from failed v0.3.6.x patches.

Verified root cause of dead Campaign button:
The previous menu markup was appended after <script src="game.js">, so game.js executed
before campaignBtn/pauseMenu existed. Null-safe listeners prevented a crash but also meant
Campaign never received a listener.

v0.4.2:
- Menu DOM is present before game.js loads.
- Campaign uses the existing verified deploy() mission initializer.
- Pause freezes RAF simulation.
- Resume compensates mission start/spawn/movement timestamps for paused duration so the
  mission clock does not jump forward.
- Resume, Restart, Abort to Menu, and ESC pause are wired.
- Built directly on v0.3.5 OA KESTREL/nomenclature/gameplay baseline.
